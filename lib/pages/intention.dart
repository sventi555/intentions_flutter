import 'package:flutter/material.dart';

class Intention extends StatelessWidget {
  const Intention({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Text("Intention page"));
  }
}
